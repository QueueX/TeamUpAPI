spring:
  application:
    name: "Eureka-Server"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    fetch-registry: false
    register-with-eureka: false

server:
  port: 8081
